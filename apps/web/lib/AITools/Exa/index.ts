import { ExaAnswer } from "./answer"
import { ExaWebSearch } from "./webSearch"

export const ExaTools = {
  exaWebSearch: ExaWebSearch,
  exaAnswer: ExaAnswer,
}

export const ExaToolsKeys = Object.keys(ExaTools)
