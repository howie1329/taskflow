{
  "name": "@taskflow/context-compaction",
  "version": "1.0.0",
  "description": "Chat context compaction and summarization for Taskflow. Rolling summaries, structured thread state, and model-agnostic generation.",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run clean && npm run build"
  },
  "keywords": [
    "taskflow",
    "chat",
    "compaction",
    "summarization",
    "context"
  ],
  "author": "Howard Thomas",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "dependencies": {
    "ai": "^6.0.116",
    "gpt-tokenizer": "^3.4.0",
    "zod": "^4.1.5"
  },
  "peerDependencies": {
    "ai": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
